<%= form_with(model: class_name, local: true) do |form| %>
  <% if class_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(class_name.errors.count, "error") %> prohibited this class_name from being saved:</h2>

      <ul>
      <% class_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
